Alles klar, Ozan — du willst ein Podman‑Image, das perfekt für zukünftige GTKD‑Entwicklungen vorbereitet ist. Also ein Entwicklungs‑Container, der:

- DMD + DUB enthält  
- GTK 3 / GTK 4 Dev‑Libraries installiert  
- gtkd sofort kompilieren kann  
- vibe.d‑freie, GUI‑fokussierte Umgebung bietet  
- ideal für Podman Desktop oder Dev‑Container ist  

Ich gebe dir jetzt ein professionelles, sauberes und erweiterbares Base‑Image, das du für jedes GTKD‑Projekt wiederverwenden kannst.

---

🧱 Dockerfile: „dmd-gtkd-dev“

Dieses Image enthält alles, was du für GTKD brauchst:

- GTK 3 Development Headers  
- Cairo, Pango, GDK‑Pixbuf  
- pkg‑config  
- DMD + DUB  
- GTKD wird einmal vorgebaut (Cache warm)

---

Dockerfile

`Dockerfile
FROM ubuntu:24.04

Basis-Tools
RUN apt-get update && apt-get install -y \
    wget \
    git \
    gcc \
    g++ \
    make \
    pkg-config \
    ca-certificates \
    curl \
    # GTK3 Development
    libgtk-3-dev \
    libglib2.0-dev \
    libpango1.0-dev \
    libcairo2-dev \
    libgdk-pixbuf-2.0-dev \
    libatk1.0-dev \
    && rm -rf /var/lib/apt/lists/*

DMD + DUB installieren
RUN wget https://dlang.org/install.sh -O install.sh && \
    bash install.sh dmd && \
    rm install.sh

ENV PATH="/root/dlang/dmd-*/bin:${PATH}"

GTKD vorbereiten (Cache warm machen)
RUN dub fetch gtkd && dub build gtkd

Dev-User (für Podman Desktop)
RUN useradd -ms /bin/bash dev
USER dev
WORKDIR /home/dev/app

CMD ["bash"]
`

---

🚀 Image bauen

`bash
podman build -t dmd-gtkd-dev .
`

---

🧪 Testen

`bash
podman run -it dmd-gtkd-dev bash
`

Im Container:

`bash
dub init mygui -t gtkd
dub run
`

Wenn GTKD korrekt installiert ist, startet ein kleines Fenster.

---

🧩 Warum dieses Image ideal ist

| Feature | Vorteil |
|--------|---------|
| GTK3‑Dev‑Libraries | GTKD kann sofort kompilieren |
| pkg‑config | GTK‑Bibliotheken werden korrekt gefunden |
| DMD + DUB | Vollständige D‑Entwicklungsumgebung |
| GTKD vorgebaut | schnellere Builds |
| Dev‑User | perfekt für Podman Desktop / VS Code |

---

🔧 Optional: Ich kann dir sofort erweitern

Wenn du willst, kann ich dir direkt eine Variante bauen:

1. GTKD + LDC (für Release‑Builds)

2. GTKD + vibe.d (für hybride GUI + HTTP‑Tools)

3. GTKD + Flatpak‑SDK (für GNOME‑Integration)

4. GTKD + Podman‑D‑SDK (für GUI‑Container‑Management)

5. Ein komplettes GTKD‑Projekt‑Template

6. Ein Podman‑Compose‑Setup für GUI‑Apps

Sag mir einfach, wie du dein GTKD‑Setup weiterentwickeln willst — ich passe das Image exakt an deinen Workflow an.