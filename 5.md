Alles klar, Ozan — du willst ein Container‑Image, das sowohl für DMD‑Entwicklung als auch für Podman Desktop (also als Dev‑Container / Build‑Umgebung) taugt. Kein Bild im visuellen Sinne, sondern ein Base‑Image, das du in Podman Desktop als Entwicklungsumgebung nutzen kannst.

Ich gebe dir jetzt ein professionelles, erweiterbares Image, das perfekt zu deinem modularen Stil passt:

- DMD + DUB  
- vibe.d‑ready (SSL + libevent)  
- Build‑Tools  
- Optional: VS Code / Podman Desktop Dev‑Container‑Kompatibilität  
- Saubere Struktur für spätere Microservices  

---

🧱 Dockerfile: „dmd-dev-desktop“

Dieses Image ist ideal für Podman Desktop, Dev‑Container, Microservices und lokale Entwicklung.

`Dockerfile
FROM ubuntu:24.04

Basis-Tools
RUN apt-get update && apt-get install -y \
    wget \
    git \
    gcc \
    g++ \
    make \
    pkg-config \
    ca-certificates \
    libssl-dev \
    libevent-dev \
    curl \
    && rm -rf /var/lib/apt/lists/*

DMD + DUB installieren
RUN wget https://dlang.org/install.sh -O install.sh && \
    bash install.sh dmd && \
    rm install.sh

ENV PATH="/root/dlang/dmd-*/bin:${PATH}"

vibe.d vorbereiten (Cache warm machen)
RUN dub fetch vibe-d && dub build vibe-d

Dev-User (für Podman Desktop / VS Code)
RUN useradd -ms /bin/bash dev
USER dev
WORKDIR /home/dev/app

CMD ["bash"]
`

---

🚀 Image bauen

`bash
podman build -t dmd-dev-desktop .
`

---

🧪 Testen

`bash
podman run -it dmd-dev-desktop bash
`

Im Container:

`bash
dmd --version
dub --version
dub init test -t vibe.d
dub run
`

---

🖥️ Perfekt für Podman Desktop

Dieses Image funktioniert ideal als:

- Dev‑Container  
- Microservice‑Base‑Image  
- Build‑Umgebung  
- Lokale Entwicklungsumgebung  

Du kannst es direkt in Podman Desktop als „Custom Image“ auswählen und damit Entwicklungscontainer starten.

---

🧩 Optional: Devcontainer‑Support (VS Code + Podman Desktop)

Wenn du willst, kann ich dir zusätzlich eine komplette:

`
.devcontainer/devcontainer.json
`

erstellen, damit Podman Desktop + VS Code automatisch:

- dein Image nutzt  
- Ports mapped  
- Extensions installiert  
- Workspace mountet  

---

🔥 Wenn du möchtest, erweitere ich das Image sofort um:

- LDC‑Compiler  
- Zsh + Oh‑My‑Zsh  
- GitHub CLI  
- MongoDB‑Client  
- Redis‑Tools  
- Systemd‑less Supervisor für Microservices  
- Multi‑Stage‑Build für Production  

Sag mir einfach, wie „luxuriös“ dein D‑Entwicklungsimage werden soll.